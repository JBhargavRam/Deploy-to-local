
name: Update Alfresco Global Properties

on:
  workflow_dispatch:
     # When this file is updated

jobs:
  update-file:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          clean: true
          sparse-checkout-cone-mode: false # Fixes the "not a directory" error
          sparse-checkout: 'config/alfresco-global.properties'

      - name: Copy File to Target Dir
        shell: cmd
        run: |
          if not exist "D:\Work\JBR\Code\ACS Target\assumedTarget\" mkdir "D:\Work\JBR\Code\ACS Target\assumedTarget\"
          xcopy "config\alfresco-global.properties" "D:\Work\JBR\Code\ACS Target\assumedTarget\" /Y

          if not exist "D:\Work\JBR\Code\ACS Target\assumedTarget\" mkdir "D:\Work\JBR\Code\ACS Target\assumedTarget\"
      
          if exist "config\1alfresco-global.properties" (
            xcopy "config\alfresco-global.properties" "D:\Work\JBR\Code\ACS Target\assumedTarget\" /Y
            echo File copied successfully.
          ) else (
            echo WARNING: config/alfresco-global.properties was not found in the repository checkout!
          )
      
     #   shell: powershell -ExecutionPolicy Bypass -Command ". '{0}'"
     #   run: |
          # init
     #     $sourceFile = "config/alfresco-global.properties"
     #     $targetPath = "D:\Work\JBR\Code\ACS Target\assumedTarget\"

      #    Copy-Item -Path $sourceFile -Destination $targetPath -Force
       #   Write-Host "File Config Updated"

      - name: Run Success Message
        shell: cmd
        run: |
          call "D:\Work\JBR\Code\ACS Target\scripts\startup.bat"
